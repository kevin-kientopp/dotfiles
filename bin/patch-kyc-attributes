#!/usr/bin/env bash

curl -X PATCH "http://localhost:8050/api/kyc/v1/288960470" \
  -d '{"idologyNameToDobToSsnMatch":{"complete":true},"idologyAddressMatch":{"complete":true}}' \
  -H 'Content-type: application/json' \
  -H "Authorization: Bearer $(issue-uaa-token)"
